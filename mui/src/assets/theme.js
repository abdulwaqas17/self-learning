import { createTheme } from "@mui/material";

const theme = createTheme({
    palette:{
        primary:{
            main: "#0f2027"
        },
        secondary : {
            main : "#2c5364"
        }
    }
});

export default theme;